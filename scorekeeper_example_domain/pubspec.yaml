name: example_domain
description: A starting point for Dart libraries or applications.
version: 0.0.1-dev
# homepage: https://www.example.com

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  scorekeeper_domain:
    path: ../scorekeeper_domain
  uuid: ^2.2.2


dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  scorekeeper_codegen:
    path: ../scorekeeper_codegen
  build_version: 2.0.1
  # Nodig om "pub run build_runner build" uit te voeren
  build_config: ^0.4.5
  build_runner: ^1.10.12
